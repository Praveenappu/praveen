#include<stdio.h>
#include<conio.h>
void main()
{
int a[10],b[10],c,d,sum,ans,add;
clrscr();
printf("the array is :\n");
scanf("%d%d",&a[c],&b[d]);
if(a[c]!=b[d])
{
printf("invalid");
}
else
{
sum=a[c];
add=b[d];
ans=sum+add;
printf("the no:%d",ans);
}
getch();
}
